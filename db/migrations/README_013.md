# Migration 013: Add Email to Profiles

## Проблема

**Симптом:** При попытке получить список студентов в админке возникает ошибка:
```
ERROR: 42703: column "email" does not exist
```

**Причина:** 
- Email пользователей хранится в системной таблице `auth.users` (управляется Supabase Auth)
- Код админ-панели пытается читать `email` из таблицы `profiles`, но этой колонки там нет
- К таблице `auth.users` нельзя напрямую обращаться через обычные SQL-запросы от клиента

**Затронутые места:**
- `/admin/students` - список учеников
- `/admin/students/[id]` - профиль ученика
- Любые другие места, где нужен email пользователя

---

## Решение

Добавить колонку `email` в таблицу `profiles` и автоматически синхронизировать её с `auth.users`.

### Что делает миграция:

1. **Добавляет колонку `email`** в таблицу `profiles`
2. **Заполняет email для существующих пользователей** из `auth.users`
3. **Обновляет триггер `handle_new_user()`** для автоматического копирования email при регистрации
4. **Добавляет триггер синхронизации** для обновления email в `profiles` при изменении в `auth.users`

---

## Как применить миграцию

### Через Supabase Dashboard:
1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое `013_add_email_to_profiles.sql`
3. Вставьте и нажмите **Run**

---

## Проверка после применения

### 1. Проверка структуры
```sql
-- Убедитесь, что колонка email добавлена
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'profiles' AND column_name = 'email';
```
**Ожидаемый результат:** 1 строка с `email | text`

### 2. Проверка заполнения email
```sql
-- Проверьте, что email заполнен для существующих пользователей
SELECT id, full_name, email, role 
FROM profiles 
WHERE email IS NOT NULL
LIMIT 10;
```
**Ожидаемый результат:** Все пользователи должны иметь email

### 3. Проверка студентов (оригинальный запрос)
```sql
-- Теперь этот запрос должен работать!
SELECT id, full_name, email, grade, created_at 
FROM profiles 
WHERE role = 'student'
ORDER BY created_at DESC;
```
**Ожидаемый результат:** Список студентов с email-адресами

### 4. Проверка триггера для новых пользователей
После создания нового пользователя через Supabase Auth UI:
```sql
SELECT id, email, role, created_at 
FROM profiles 
WHERE email = 'newemail@example.com';  -- замените на реальный email
```
**Ожидаемый результат:** Профиль создан автоматически с email

---

## Проверка через UI

### В админ-панели:

**До миграции:**
- ❌ `/admin/students` - ошибка "column email does not exist"
- ❌ Список учеников не отображается

**После миграции:**
- ✅ `/admin/students` - отображается таблица с учениками
- ✅ Колонка "Email" заполнена для всех студентов
- ✅ Можно кликнуть "Просмотр" для детального профиля

---

## Технические детали

### Добавленная колонка
```sql
ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS email TEXT;
```

### Backfill существующих данных
```sql
UPDATE profiles
SET email = auth.users.email
FROM auth.users
WHERE profiles.id = auth.users.id
  AND profiles.email IS NULL;
```
Копирует email из `auth.users` для всех существующих пользователей.

### Обновленный триггер создания пользователя
```sql
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, role, email)
  VALUES (NEW.id, 'student', NEW.email)
  ON CONFLICT (id) DO UPDATE 
    SET email = EXCLUDED.email;
  RETURN NEW;
END;
$$;
```
При создании пользователя автоматически:
- Создается запись в `profiles`
- Копируется `email` из `auth.users`
- Устанавливается роль `student` по умолчанию

### Новый триггер синхронизации email
```sql
CREATE OR REPLACE FUNCTION public.handle_user_email_update()
RETURNS TRIGGER AS $$
BEGIN
  IF NEW.email IS DISTINCT FROM OLD.email THEN
    UPDATE public.profiles
    SET email = NEW.email
    WHERE id = NEW.id;
  END IF;
  RETURN NEW;
END;
$$;
```
Если пользователь меняет email в настройках Supabase Auth, изменение автоматически синхронизируется в `profiles`.

---

## Безопасность

### Email виден только учителям и владельцу
Существующие RLS политики:
- ✅ Студент видит только свой профиль (`profiles_select_own`)
- ✅ Учитель видит профили всех студентов (`Teachers can read student profiles`)
- ✅ Публика видит только профили учителей для landing page

### Нет дублирования логики авторизации
- Email используется только для отображения
- Авторизация и управление пользователями остается на `auth.users`
- `profiles.email` - это кэш для удобства, синхронизируется автоматически

---

## Влияние на код приложения

### Теперь работают запросы:
```typescript
// В админ-панели студентов
const { data: students } = await supabase
  .from("profiles")
  .select("id, full_name, email, grade, created_at")
  .eq("role", "student");
```

### При регистрации (обновлено):
```typescript
// В app/(public)/register/page.tsx
await supabase.from("profiles").upsert({
  id: data.user.id,
  role: "student",
  full_name: fullName || null,
  email: data.user.email || null  // ← Добавлено явно
});
```

---

## Откат миграции (если необходимо)

```sql
-- Удалить триггер синхронизации
DROP TRIGGER IF EXISTS on_auth_user_email_updated ON auth.users;
DROP FUNCTION IF EXISTS public.handle_user_email_update();

-- Вернуть старую версию триггера создания пользователя
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, role)
  VALUES (NEW.id, 'student')
  ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$;

-- Удалить колонку email (ОСТОРОЖНО: потеряете данные!)
ALTER TABLE profiles DROP COLUMN IF EXISTS email;
```

**⚠️ Внимание:** После отката админ-панель снова перестанет работать!

---

## Порядок применения миграций

1. ✅ `001-011` - Основная структура БД
2. ✅ `012_fix_teacher_rls_policies.sql` - Исправление RLS для учителей
3. ✅ `013_add_email_to_profiles.sql` - **Эта миграция** (добавление email)

После применения миграций 012 и 013 админ-панель должна **полностью работать**! 🎉
